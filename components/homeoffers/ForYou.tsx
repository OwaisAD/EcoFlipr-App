import { SafeAreaView, ScrollView, Text, View } from "react-native";

export const ForYou = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <ScrollView>
        <View>
          <Text>For you</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
};
